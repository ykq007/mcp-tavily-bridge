DATABASE_URL="file:./tavily_bridge.db"
KEY_ENCRYPTION_SECRET="tBaUyjIKnpyPOkkPN2n/3jPypcl0HkbbDzV6IuJ7WyY="
ADMIN_API_TOKEN="2f6d35ecf4d6d54cb5bb67828173813d8ef84d04fec2f5c5"

HOST="0.0.0.0"
PORT="8787"
ENABLE_QUERY_AUTH="false"

MCP_RATE_LIMIT_PER_MINUTE="60"
MCP_GLOBAL_RATE_LIMIT_PER_MINUTE="600"
MCP_MAX_RETRIES="2"
MCP_COOLDOWN_MS="60000"

# Upstream key selection strategy: round_robin (default) | random
TAVILY_KEY_SELECTION_STRATEGY="round_robin"

# Tavily usage logging (Admin UI → Usage)
# - none: do not record usage
# - hash: record queryHash only
# - preview: record queryHash + redacted queryPreview (default)
# - full: record queryHash + full (still redacted) queryPreview
TAVILY_USAGE_LOG_MODE="preview"

# Optional but recommended: makes query hashes keyed (HMAC-SHA256) instead of plain SHA256
TAVILY_USAGE_HASH_SECRET=""

# Optional sampling rate for usage events (0..1). Empty means log everything.
TAVILY_USAGE_SAMPLE_RATE=""

# Optional retention policy. If set, the server will probabilistically delete old usage rows.
TAVILY_USAGE_RETENTION_DAYS=""
TAVILY_USAGE_CLEANUP_PROBABILITY="0.001"
